tree-sitter-bash 0.25.1 release WASM (https://github.com/tree-sitter/tree-sitter-bash), unmodified.
Loaded by ../bash-parser.ts with web-tree-sitter; bump both together (the grammar ABI must be one the runtime supports).
